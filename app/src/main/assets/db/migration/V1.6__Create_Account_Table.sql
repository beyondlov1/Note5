CREATE TABLE ACCOUNT (
                ID TEXT PRIMARY KEY NOT NULL ,
                SERVER TEXT,
                USERNAME TEXT,
                PASSWORD TEXT,
                PRIORITY INTEGER,
                VALID INTEGER);